
P — количество параметров модели в миллиардах.
GPU: NVIDIA H100.
Производительность H100: 989 TFLOPS.
Стоимость H100: $1.85 за GPU-час.
Эффективность обучения: 40%.
Количество обучающих токенов: 20 токенов на параметр.

Формулы:

training_tokens = 20 × P × 10^9

training_FLOPs = 6 × (P × 10^9) × training_tokens

GPU_hours =
    training_FLOPs /
    (989 × 10^12 × 3600 × 0.4)

training_cost_USD = GPU_hours × 1.85
